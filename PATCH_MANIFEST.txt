yourDietManager taxonomy alignment bootstrap hotfix

Apply on top of: yourDietManager-taxonomy-alignment-patch.zip

Changed files:
- public/service-worker.js
- dist/service-worker.js

Fixes:
- bump shell cache ydm-shell-v46 -> ydm-shell-v47
- activate updated worker immediately via self.skipWaiting()
- load schema files network-first with cached offline fallback

Reason:
The taxonomy patch updated ingredient schemas to allow taxonomy.culinaryRoles,
but the previous service worker could keep serving the old cached schema while
loading the new catalog from the network, causing bootstrap validation failure.
